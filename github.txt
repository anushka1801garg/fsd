git is a software and github is a service that is used to stor all what we are doing using git on cloud.

like github there are many service such as gitbucket.

role of git is to version control:track the changes in the file and if something wrong happens then we can trace back to it.it is used for checkpointing.

not every folder is being tracked by the git,ou need to initiliaze it so that it can be tracked by the git.

COMMAND1:
  "git status":to check the status ,if error intially then okayy.

  then go into the folder and write "git init"(one time per project)
  git init make a folder ".git" which is hidden to keep history of all files and sub-folders.

  "git add file_name" or "git add." to make everything add.
  "git commit -m "a message""

"git log" shows commit id and other inforation(such as author name,email id,date) of commits done.  

example

PS C:\Users\Anushka Garg\Desktop\fsd\github_practise> git log
commit e6b3fac902a9557de44cdd0a71fee4a2b44d4843 (HEAD -> master)
Author: Anushka Garg <your_github_email@example.com>
Date:   Wed Jun 10 12:53:59 2026 +0530

"git log --oneline"  shows the commit id

example

PS C:\Users\Anushka Garg\Desktop\fsd\github_practise> git log --oneline
e6b3fac (HEAD -> master) github guide

git config --global user.name "name of the user"
git config --global user.email "email"  

git config --global core.editor "code --wait"  (if you only want to write commit and use it in vs code rather than vim)

.env file is created to store the personal info
.gitignore file is created to include the file that we don't want to show to all on github

you can use gitignore generator to append most commmonly ignored file

"C:\Users\Anushka Garg\.gitconfig"(to see configuration file)